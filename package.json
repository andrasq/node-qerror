{
  "name": "qerror",
  "version": "0.1.0",
  "description": "error handler hook for fatal signals",
  "main": "qerror.js",
  "license": "Apache-2.0",
  "author": "Andras",
  "repository": "git://github.com:andrasq/node-qerror",
  "scripts": {
    "test": "node test-qerror.js",
    "coverage": "nyc --reporter text --reporter lcov npm test",
    "clean": "rm -rf .nyc_output coverage"
  },
  "dependencies": {
  },
  "devDependencies": {
  }
}
